import { makeStyles } from "@material-ui/styles";

export default makeStyles({
    root: {
        textTransform: "none",
        fontFamily: "Nunito",
        backgroundColor: "none",
        borderRadius: 10,
        color: "none",
        "&:hover": {
            backgroundColor: "none",
        },
        whiteSpace: "nowrap",
    },
});
